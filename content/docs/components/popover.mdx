---
title: Popover
description: HTML Popover APIを使用したネイティブなポップオーバーコンポーネント
---

import { Popover, PopoverContent, PopoverTrigger } from "@/registry/ui/popover";
import { Card, CardContent } from "@/registry/ui/card";
import { TypeTable } from "fumadocs-ui/components/type-table";

## 概要

PopoverコンポーネントはHTML Popover APIを使用し、`popovertarget`属性と`popover`属性を活用したネイティブなポップオーバー機能を提供します。JavaScriptなしで動作し、アクセシビリティに優れています。

## デモ

### 基本的な使い方

export const BasicDemo = () => (
  <div className="my-6 flex gap-4">
    <PopoverTrigger target="basic-popover">
      ポップオーバーを開く
    </PopoverTrigger>
    <Popover id="basic-popover">
      <PopoverContent>
        <h3 className="font-semibold mb-2">ポップオーバーのタイトル</h3>
        <p className="text-sm text-muted-foreground">
          これは基本的なポップオーバーコンテンツです。外側をクリックするか、ESCキーを押すと閉じます。
        </p>
      </PopoverContent>
    </Popover>
  </div>
);

<BasicDemo />

```tsx
import { Popover, PopoverContent, PopoverTrigger } from "@/registry/ui/popover"

export default function Example() {
  return (
    <div>
      <PopoverTrigger target="my-popover">
        クリックして開く
      </PopoverTrigger>
      
      <Popover id="my-popover">
        <PopoverContent>
          <h3>タイトル</h3>
          <p>コンテンツがここに入ります</p>
        </PopoverContent>
      </Popover>
    </div>
  )
}
```

### 異なるバリアント

export const VariantsDemo = () => (
  <div className="my-6 flex flex-wrap gap-4">
    <PopoverTrigger target="outline-popover" variant="outline">
      アウトライン
    </PopoverTrigger>
    <Popover id="outline-popover">
      <PopoverContent>
        <p className="text-sm">デフォルトスタイルのポップオーバー</p>
      </PopoverContent>
    </Popover>

    <PopoverTrigger target="secondary-popover" variant="secondary">
      セカンダリー
    </PopoverTrigger>
    <Popover id="secondary-popover" variant="accent">
      <PopoverContent>
        <p className="text-sm">アクセントスタイルのポップオーバー</p>
      </PopoverContent>
    </Popover>

    <PopoverTrigger target="destructive-popover" variant="destructive">
      デストラクティブ
    </PopoverTrigger>
    <Popover id="destructive-popover">
      <PopoverContent>
        <h3 className="font-semibold mb-2 text-destructive">警告</h3>
        <p className="text-sm text-muted-foreground">
          この操作は取り消せません。
        </p>
      </PopoverContent>
    </Popover>
  </div>
);

<VariantsDemo />

```tsx
<PopoverTrigger target="my-popover" variant="outline">
  アウトライン
</PopoverTrigger>
<Popover id="my-popover" variant="accent">
  <PopoverContent>
    アクセントスタイル
  </PopoverContent>
</Popover>
```

### 異なるサイズ

export const SizesDemo = () => (
  <div className="my-6 flex flex-wrap gap-4">
    <PopoverTrigger target="sm-popover" size="sm">
      小
    </PopoverTrigger>
    <Popover id="sm-popover" size="sm">
      <PopoverContent spacing="sm">
        <p className="text-sm">小サイズのポップオーバー</p>
      </PopoverContent>
    </Popover>

    <PopoverTrigger target="default-popover">
      デフォルト
    </PopoverTrigger>
    <Popover id="default-popover">
      <PopoverContent>
        <p className="text-sm">デフォルトサイズのポップオーバー</p>
      </PopoverContent>
    </Popover>

    <PopoverTrigger target="lg-popover" size="lg">
      大
    </PopoverTrigger>
    <Popover id="lg-popover" size="lg">
      <PopoverContent spacing="lg">
        <p className="text-sm">
          大サイズのポップオーバー。より多くのコンテンツを含めることができます。
        </p>
      </PopoverContent>
    </Popover>
  </div>
);

<SizesDemo />

```tsx
<PopoverTrigger target="sm-popover" size="sm">
  小
</PopoverTrigger>
<Popover id="sm-popover" size="sm">
  <PopoverContent spacing="sm">
    <p className="text-sm">小サイズのポップオーバー</p>
  </PopoverContent>
</Popover>
```

### Show/Hide アクション

export const ActionsDemo = () => (
  <div className="my-6 flex gap-4">
    <PopoverTrigger target="action-popover" action="show" variant="outline">
      開く
    </PopoverTrigger>
    <PopoverTrigger target="action-popover" action="hide" variant="secondary">
      閉じる
    </PopoverTrigger>
    <Popover id="action-popover">
      <PopoverContent>
        <h3 className="font-semibold mb-2">制御されたポップオーバー</h3>
        <p className="text-sm text-muted-foreground mb-4">
          専用のボタンで開閉を制御できます。
        </p>
        <PopoverTrigger
          target="action-popover"
          action="hide"
          variant="outline"
          size="sm"
        >
          内側から閉じる
        </PopoverTrigger>
      </PopoverContent>
    </Popover>
  </div>
);

<ActionsDemo />

```tsx
<PopoverTrigger target="controlled-popover" action="show">
  開く
</PopoverTrigger>
<PopoverTrigger target="controlled-popover" action="hide">
  閉じる
</PopoverTrigger>
<Popover id="controlled-popover">
  <PopoverContent>
    専用のボタンで開閉を制御
  </PopoverContent>
</Popover>
```

### カード内のポップオーバー

export const CardDemo = () => (
  <div className="my-6 flex gap-4">
    <PopoverTrigger target="card-popover" variant="outline">
      ユーザー情報を表示
    </PopoverTrigger>
    <Popover id="card-popover" size="sm">
      <PopoverContent spacing="none">
        <Card className="border-0 shadow-none">
          <CardContent className="p-4">
            <div className="flex items-center gap-3 mb-3">
              <img
                src="https://avatar.vercel.sh/user"
                alt="ユーザー"
                className="size-12 rounded-full"
              />
              <div>
                <p className="font-semibold">山田太郎</p>
                <p className="text-sm text-muted-foreground">@yamada</p>
              </div>
            </div>
            <p className="text-sm text-muted-foreground">
              ソフトウェアエンジニア。React と TypeScript が好きです。
            </p>
          </CardContent>
        </Card>
      </PopoverContent>
    </Popover>
  </div>
);

<CardDemo />

```tsx
<Popover id="my-popover">
  <PopoverContent spacing="none">
    パディングなしのコンテンツ
  </PopoverContent>
</Popover>
```

### リッチコンテンツ

export const RichDemo = () => (
  <div className="my-6 flex gap-4">
    <PopoverTrigger target="rich-popover" variant="outline">
      詳細を表示
    </PopoverTrigger>
    <Popover id="rich-popover" size="lg">
      <PopoverContent spacing="lg">
        <h3 className="text-lg font-bold mb-3">機能リスト</h3>
        <ul className="space-y-2 mb-4">
          <li className="flex items-start gap-2">
            <span className="text-green-500 mt-0.5">✓</span>
            <span className="text-sm">ネイティブHTML Popover API</span>
          </li>
          <li className="flex items-start gap-2">
            <span className="text-green-500 mt-0.5">✓</span>
            <span className="text-sm">JavaScriptなしで動作</span>
          </li>
          <li className="flex items-start gap-2">
            <span className="text-green-500 mt-0.5">✓</span>
            <span className="text-sm">アクセシビリティ対応</span>
          </li>
          <li className="flex items-start gap-2">
            <span className="text-green-500 mt-0.5">✓</span>
            <span className="text-sm">カスタマイズ可能なスタイル</span>
          </li>
        </ul>
        <div className="flex gap-2">
          <PopoverTrigger
            target="rich-popover"
            action="hide"
            variant="default"
            size="sm"
          >
            了解
          </PopoverTrigger>
          <PopoverTrigger
            target="rich-popover"
            action="hide"
            variant="outline"
            size="sm"
          >
            キャンセル
          </PopoverTrigger>
        </div>
      </PopoverContent>
    </Popover>
  </div>
);

<RichDemo />

```tsx
<PopoverTrigger target="rich-popover" variant="outline">
  詳細を表示
</PopoverTrigger>
<Popover id="rich-popover" size="lg">
  <PopoverContent spacing="lg">
    <h3 className="text-lg font-bold mb-3">機能リスト</h3>
    <ul className="space-y-2 mb-4">
      <li>✓ ネイティブHTML Popover API</li>
      <li>✓ JavaScriptなしで動作</li>
      <li>✓ アクセシビリティ対応</li>
    </ul>
  </PopoverContent>
</Popover>
```

## インストール

```bash
npx shadcn@latest add https://gyoza-ui.reichima.com/r/popover.json
```

## Props

### Popover

<TypeTable
  type={{
    id: {
      description: "ポップオーバーの一意のID",
      type: "string",
      required: true,
    },
    variant: {
      description: "ポップオーバーのスタイルバリアント",
      type: '"default" | "accent"',
      default: '"default"',
    },
    size: {
      description: "ポップオーバーの最大幅",
      type: '"sm" | "default" | "lg" | "xl"',
      default: '"default"',
    },
  }}
/>

### PopoverTrigger

<TypeTable
  type={{
    target: {
      description: "ターゲットとなるポップオーバーのID",
      type: "string",
      required: true,
    },
    action: {
      description: "ボタンの動作",
      type: '"toggle" | "show" | "hide"',
      default: '"toggle"',
    },
    variant: {
      description: "ボタンのスタイルバリアント",
      type: '"default" | "destructive" | "outline" | "secondary" | "ghost" | "link"',
      default: '"default"',
    },
    size: {
      description: "ボタンのサイズ",
      type: '"default" | "sm" | "lg" | "icon"',
      default: '"default"',
    },
  }}
/>

### PopoverContent

<TypeTable
  type={{
    spacing: {
      description: "コンテンツのパディング",
      type: '"none" | "sm" | "default" | "lg"',
      default: '"default"',
    },
  }}
/>

## ネイティブ機能

HTML Popover APIにより、以下の機能が自動的に提供されます:

- ✅ **自動フォーカス管理**: ポップオーバーが開くと適切な要素にフォーカスが移動
- ✅ **ESCキーで閉じる**: ESCキーを押すとポップオーバーが閉じます
- ✅ **外側クリックで閉じる**: ポップオーバーの外側をクリックすると自動的に閉じます
- ✅ **バックドロップサポート**: `::backdrop`疑似要素でバックドロップをスタイリング可能
- ✅ **アクセシビリティ**: ネイティブHTMLセマンティクスによる優れたアクセシビリティ

## ブラウザ互換性

HTML Popover APIは以下のブラウザでサポートされています:

- ✅ Chrome 114+
- ✅ Edge 114+
- ✅ Safari 17+
- ⚠️ Firefox (実験的サポート)

## 技術的詳細

- `class-variance-authority`でバリアント管理
- Tailwind CSSでスタイリング
- ネイティブHTML Popover API
- JavaScriptなしで動作

## 注意事項

### TypeScript型

`popover`、`popoverTarget`、`popoverTargetAction`属性はReactの型定義に含まれていますが、一部の環境では型エラーが発生する可能性があります。その場合は、`@ts-ignore`コメントを追加してください。

### カスタマイズ

コンポーネントは`class-variance-authority`を使用しているため、既存のバリアントを拡張したり、新しいバリアントを追加することが簡単にできます。

```tsx
// カスタムバリアントを追加する例
const customPopoverVariants = cva(
  // ベースクラス
  "...",
  {
    variants: {
      variant: {
        default: "...",
        accent: "...",
        // 新しいバリアントを追加
        custom: "border-purple-500 bg-purple-50",
      },
    },
  }
)
```
